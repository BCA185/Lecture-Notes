# Unit 3 Day 1

Today we will find out what CSS is and have Three in class example of different types of CSS

## What is CSS

- [CSS Mozilla](https://developer.mozilla.org/en-US/docs/Web/CSS)

## Using CSS

You can add CSS to HTML in three ways.
External is the most common but we will show/use all three.
[HTML Styles](https://www.w3schools.com/html/html_css.asp)

### Inline

Apply CSS directly to an element.

- [Example](./InlineCSS.html)

### Internal

Used to define style for a single HTML page.
For Internal CSS put all style put in `<style>` within `<head>`

- This also shows the use of ID's
- [Internal Example](./InternalCSS.html)

### External

The same as Internal but in its own file linked with `<link>` tag within the `<head>`

- For this example copy the HTML and remove the CSS to its own file.
- [External Example](./ExtneralExample.html)

EOD
