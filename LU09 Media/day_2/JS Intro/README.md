# Unit 9 Day 2 Into to JS

First watch [this video](https://www.youtube.com/watch?v=DHjqpvDnNGE) by Fireship

## First look at JS

### Using JS in Browser

Open web browser console have them enter the following:

```
console.log('Hello', 'World', '!')
console.log('HAPPY', 'NEW', 'YEAR', 2020)
console.log('Welcome', 'to', 30, 'Days', 'Of', 'JavaScript')
```

Math in browser:

```
console.log(2 + 3) // Addition
console.log(3 - 2) // Subtraction
console.log(2 * 3) // Multiplication
console.log(3 / 2) // Division
console.log(3 % 2) // Modulus - finding remainder
console.log(3 ** 2) // Exponentiation 3 ** 2 == 3 * 3
```

## Adding JS to a Website

-   now in the browser make index.html
-   now make index2.html
    note how we used an internal JavaScript
-   now lets use an external JavaScript _ make interduction.JavaScript_ make now make index3.htm
-   move the external script to the body
-   now lets make a scond script* build helloworld.js * add the script above interduction.js
-   Show how to Seach DOM* build searchDOM.html* build searchDOM.js
-   Move on to adding two numbers example
    build addTwoNumber.html
    build addTwoNum.js
